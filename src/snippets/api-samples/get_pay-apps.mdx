import { Tabs, TabItem } from "@astrojs/starlight/components";

<Tabs>
<TabItem label="curl">

```bash
curl -X GET https://app.buildworkpro.com/api/v1/pay-apps \
  -H "Authorization: Bearer ${API_KEY}"
```

</TabItem>
<TabItem label="JavaScript">

```js
const res = await fetch("https://app.buildworkpro.com/api/v1/pay-apps", {
  method: "GET",
  headers: {
    "Authorization": `Bearer ${process.env.BUILDWORKPRO_API_KEY}`,
  },
});
const data = await res.json();
```

</TabItem>
<TabItem label="Python">

```python
import os
import requests

res = requests.get(
  "https://app.buildworkpro.com/api/v1/pay-apps",
  headers={
    "Authorization": f"Bearer {os.environ['BUILDWORKPRO_API_KEY']}",
  },
)
data = res.json()
```

</TabItem>
</Tabs>
